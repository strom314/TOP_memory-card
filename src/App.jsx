import { useState } from "react";

import "./App.css";

function App() {
  /*
  [
  id:
  img:
  clicked:
  ]

  score
  highScore

  handleClick(e){
  id = cards[e.target.id]
  if(cards[id].clicked){
    restartGame()
  }else{
    cards[id].clicked = true;
    }
  }
*/
  return (
    <>
      {/* cards.map(() => {
        return <Card img= handleClick= id=>
      }) 
       */}
    </>
  );
}

export default App;
